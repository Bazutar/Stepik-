#include <stdio.h>

int main() {
	int i = 0, N = 0, n = 0, a = 0, b = 0, c = 0, d = 0, arr_num[100] = { 0 };

	scanf("%d", &N);
	b = N - 1;

	while (i < N) {
		scanf("%d", &n);
		arr_num[i] = n;
		i++;
	}
	while (a < N) {
		if ((arr_num[a] < arr_num[b]) && (arr_num[a] > arr_num[d])) {
			printf("%d ", arr_num[a]);
			c++;
		}
		a++;
	}
	if (c == 0) {
		printf("0");
	}
return 0;
}
//RTFM
